# Display names for RedundancyLevel enum values.
# Key = enum constant name or legacy alias; Value = display label.
# "RAID 6" is a legacy alias kept for backward compatibility ? new code uses "ErasureCoding".
RAID 0=RAID 0
RAID 1=RAID 1
RAID 6=ErasureCoding
ErasureCoding=ErasureCoding